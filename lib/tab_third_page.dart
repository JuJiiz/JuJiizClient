import 'package:flutter/material.dart';

class TabThirdPage extends StatelessWidget {
  const TabThirdPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.brown,
      width: double.infinity,
      height: double.infinity,
    );
  }
}
